import 'package:flutter/material.dart';
import 'package:material_design_icons_flutter/material_design_icons_flutter.dart';

class Category {
  String name;
  int color;
  IconData icon;
  int iconColor;
  String iconPath;

  Category({this.name, this.color, this.icon, this.iconColor, this.iconPath});
}

